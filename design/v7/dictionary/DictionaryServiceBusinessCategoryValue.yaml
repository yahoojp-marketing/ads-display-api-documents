DictionaryServiceBusinessCategoryValue:
  type: object
  nullable: true
  properties:
    adCategory:
      $ref: 'DictionaryServiceBusinessCategory.yaml#/DictionaryServiceBusinessCategory'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: DictionaryServiceBusinessCategoryValue
  description: |
    <div lang="ja">DictionaryServiceBusinessCategoryValueオブジェクトは、ビジネスカテゴリー情報を取得するコンテナです。</div>
    <div lang="en">DictionaryServiceBusinessCategoryValue object is a container that retrieves business category information.</div>
