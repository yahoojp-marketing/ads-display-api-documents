DictionaryServiceMediaAdFormatValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    mediaAdFormat:
      type: array
      nullable: true
      items:
        $ref: 'DictionaryServiceMediaAdFormat.yaml#/DictionaryServiceMediaAdFormat'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div><div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: DictionaryServiceMediaAdFormatValue
  description: |
    <div lang="ja">DictionaryServiceMediaAdFormatValueオブジェクトは、getMediaAdFormatメソッドの実行結果（１Entity）を保持します。</div><div lang="en">DictionaryServiceMediaAdFormatValue object contains the results (1 Entity) for getMediaAdFormat method.</div>
