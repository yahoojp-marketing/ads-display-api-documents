MediaServiceLogoFlg:
  type: string
  nullable: true
  enum:
  - 'FALSE'
  - 'TRUE'
  - UNKNOWN
  title: MediaServiceLogoFlg
  description: |
    <div lang="ja">MediaServiceLogoFlgは、ロゴの設定フラグを表します。</div>
    <div lang="en">MediaServiceLogoFlg displays the flag settings of Logo image.</div>
    <hr>
    <p>* <code>FALSE</code> - <span lang="ja">通常画像です。</span><span lang="en">Normal image.</span></p>
    <p>* <code>TRUE</code> - <span lang="ja">ロゴ画像です。</span><span lang="en">Logo image.</span></p>
    <p>* <code>UNKNOWN</code> - <span lang="ja">未知の値です。</span><span lang="en">Unknown Value</span></p>
