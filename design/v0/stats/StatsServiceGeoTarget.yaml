StatsServiceGeoTarget:
  type: object
  nullable: true
  properties:
    geoNameEn:
      type: string
      nullable: true
      description: |
        <div lang="ja">地域名（英語）</div>
        <div lang="en">Geo name(English)</div>
    geoNameJa:
      type: string
      nullable: true
      description: |
        <div lang="ja">地域名（日本語）</div>
        <div lang="en">Geo name(Japanese)</div>
  title: StatsServiceGeoTarget
  description: |
    <div lang="ja">StatsServiceGeoTargetオブジェクトは、地域ターゲットの設定情報を保持します。</div>
    <div lang="en">StatsServiceGeoTarget object holds configuration information of Geo targeting.</div>
