AdGroupAdServiceValue:
  type: object
  nullable: true
  properties:
    adGroupAd:
      $ref: 'AdGroupAd.yaml#/AdGroupAd'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div><div lang="en">The
                    process results. If true, the process succeeded. If false, the process failed.</div>
  title: AdGroupAdServiceValue
  description: |
    <div lang="ja">AdGroupAdServiceValueオブジェクトは、処理結果を含む広告に関する情報を表します。</div><div lang="en">The AdGroupAdServiceValue object is a container for storing ad information includes operation results.</div>
