AdGroupServiceEligibilityFlg:
  type: string
  nullable: true
  enum:
  - ENABLE
  - DISABLE
  - UNKNOWN
  title: AdGroupServiceEligibilityFlg
  description: |
    <div lang="ja">AdGroupServiceEligibilityFlgは、コンバージョン最適化利用可否フラグを表します。</div>
    <div lang="en">AdGroupServiceEligibilityFlg serves if conversion optimization is valid.</div>
    <hr>
    <p>* <code>ENABLE</code> - <span lang="ja">コンバージョン最適化利用可能</span><span lang="en">Can use the Conversion Optimization functions.</span></p>
    <p>* <code>DISABLE</code> - <span lang="ja">コンバージョン最適化利用不可</span><span lang="en">Cannot use the Conversion Optimization functions.</span></p>
    <p>* <code>UNKNOWN</code> - <span lang="ja">未知の値です。</span><span lang="en">Unknown Value</span></p>
