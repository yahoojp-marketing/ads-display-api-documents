VideoServiceValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
    video:
      $ref: 'Video.yaml#/Video'
  title: VideoServiceValue
  description: |
    <div lang="ja">VideoServiceValueオブジェクトは、メソッドの実行結果（1Entity）を保持します。</div><div lang="en">VideoServiceValue objects stores the execution result (1 Entity) of method.</div><hr>
    <details><summary>Requirement</summary>
    <table border='1'>
      <tr>
        <th>PROPERTY</th>
        <th>GET</th>
        <th>SET</th>
        <th>REMOVE</th>
      </tr>
      <tr>
        <td>video</td>
        <td>-</td>
        <td>-</td>
        <td>-</td>
      </tr>
    </table></details>
