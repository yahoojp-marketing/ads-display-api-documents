GuaranteedSimulationServicePackageValue:
  type: object
  nullable: true
  properties:
    package:
      $ref: 'GuaranteedSimulationServicePackage.yaml#/GuaranteedSimulationServicePackage'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: GuaranteedSimulationServicePackageValue
  description: |
    <div lang="ja">GuaranteedSimulationServicePackageValueオブジェクトは、商品情報を取得するコンテナです。</div>
    <div lang="en">GuaranteedSimulationServicePackageValue object is a container that retrieves package information.</div>
