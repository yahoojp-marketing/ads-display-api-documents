AccountAdProductServiceValue:
  type: object
  nullable: true
  properties:
    accountAdProduct:
      $ref: 'AccountAdProduct.yaml#/AccountAdProduct'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
      description: |
        <div lang="ja">エラー内容です。</div><div lang="en">Error content.</div>
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div><div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: AccountAdProductServiceValue
  description: |
    <div lang="ja">AccountAdProductServiceValueオブジェクトは、広告掲載方式を含むコンテナです。</div><div lang="en">AccountAdProductServiceValue object that includes the ad display methods.</div>
