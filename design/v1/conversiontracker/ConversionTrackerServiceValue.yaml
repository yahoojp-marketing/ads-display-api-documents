ConversionTrackerServiceValue:
  type: object
  nullable: true
  properties:
    conversionTracker:
      $ref: 'ConversionTracker.yaml#/ConversionTracker'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
    statsPeriodCustomDate:
      $ref: 'ConversionTrackerServiceStatsPeriodCustomDate.yaml#/ConversionTrackerServiceStatsPeriodCustomDate'
  title: ConversionTrackerServiceValue
  description: |
    <div lang="ja">ConversionTrackerServiceValueオブジェクトは、操作結果を含むコンバージョントラッカー情報を表します。</div>
    <div lang="en">ConversionTrackerServiceValue object displays the entity of Conversion Tracker information.</div>
