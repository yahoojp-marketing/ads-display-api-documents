FeedSetServiceValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    feedSet:
      $ref: 'FeedSet.yaml#/FeedSet'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: FeedSetServiceValue
  description: |
    <div lang="ja">FeedSetServiceValueオブジェクトは、操作結果を含む商品セット情報を表します。</div>
    <div lang="en">FeedSetServiceValue object describes Item Set information including operation result.</div>
