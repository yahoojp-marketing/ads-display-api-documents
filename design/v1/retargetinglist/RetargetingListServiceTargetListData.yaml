RetargetingListServiceTargetListData:
  type: object
  nullable: true
  properties:
    targetListId:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">ターゲットリストIDです。<br>このフィールドは、ADDおよびSET時に必須となります。</div>
        <div lang="en">Target list ID.<br>This field is required in ADD and SET operation.</div>
    targetListName:
      type: string
      nullable: true
      description: |
        <div lang="ja">ターゲットリストの名前です。</div><div lang="en">Name of target list.</div>
  title: RetargetingListServiceTargetListData
  description: |
    <div lang="ja">RetargetingListServiceTargetListDataオブジェクトは、ターゲットリストを表します。<br>このフィールドは、ADDおよびSET時に必須となり、REMOVE時に無視されます。</div>
    <div lang="en">RetargetingListServiceTargetListData object displays the target list. <br>This field is required in ADD and SET operation, and will be ignored in REMOVE operation.</div>
    
