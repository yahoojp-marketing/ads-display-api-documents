AuditLogServiceJobStatus:
  type: string
  nullable: true
  enum:
  - IN_PROGRESS
  - COMPLETED
  - SYSTEM_ERROR
  - UNKNOWN
  title: AuditLogServiceJobStatus
  description: |
    <div lang="ja">AuditLogServiceJobStatusは、ダウンロードジョブの処理状態を表します。<br>このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。</div>
    <div lang="en">AuditLogServiceJobStatus serves process status of download job.<br>Although this field will be returned in the response, it will be ignored on input. </div><hr>
    <p>* <code>IN_PROGRESS</code> - <span lang="ja">処理中です。</span><span lang="en">Job is in progress.</span></p>
    <p>* <code>COMPLETED</code> - <span lang="ja">処理終了です。</span><span lang="en">Job is completed.</span></p>
    <p>* <code>SYSTEM_ERROR</code> - <span lang="ja">システムエラーです。</span><span lang="en">System error.</span></p>
    <p>* <code>UNKNOWN</code> - <span lang="ja">未知の値です。</span><span lang="en">Unknown Value</span></p>
