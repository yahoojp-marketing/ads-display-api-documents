CampaignMigrationServiceJobValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    migrationJob:
      $ref: 'CampaignMigrationServiceJob.yaml#/CampaignMigrationServiceJob'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: CampaignMigrationServiceJobValue
  description: |
    <div lang="ja">CampaignMigrationServiceJobValueオブジェクトは、マイグレーションジョブの処理状況を格納するコンテナです。</div><div lang="en">CampaignMigrationServiceJobValue object contains processing status of migration job.</div>
