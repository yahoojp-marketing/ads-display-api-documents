GuaranteedSimulationServicePermittedAdScheduleTarget:
  type: object
  nullable: true
  properties:
    targetId:
      type: string
      nullable: true
      description: |
        <div lang="ja">ターゲットIDです。</div>
        <div lang="en">Target ID.</div>
    dayOfWeek:
      $ref: 'GuaranteedSimulationServiceDayOfWeek.yaml#/GuaranteedSimulationServiceDayOfWeek'
    endHour:
      type: integer
      nullable: true
      format: int32
      description: |
        <div lang="ja">終了時間(時のみ)です。</div>
        <div lang="en">End time (hour only).</div>
    startHour:
      type: integer
      nullable: true
      format: int32
      description: |
        <div lang="ja">開始時間(時のみ)です。</div>
        <div lang="en">Start time (hour only).</div>
    price:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">
          固定価格です。
        </div>
        <div lang="en">
          Fixed price.
        </div>
  title: GuaranteedSimulationServicePermittedAdScheduleTarget
  description: |
    <div lang="ja">GuaranteedSimulationServicePermittedAdScheduleTargetオブジェクトは、時間帯ターゲットの設定情報を保持します。</div>
    <div lang="en">GuaranteedSimulationServicePermittedAdScheduleTarget object is a container for storing day of week/hours targeting settings.</div>
