EstimatedUserSizeServiceValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
      description: |
        <div lang="ja">エラー内容です。</div>
        <div lang="en">Error content.</div>
    estimatedUserSize:
      $ref: 'EstimatedUserSize.yaml#/EstimatedUserSize'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: EstimatedUserSizeServiceValue
  description: |
    <div lang="ja">EstimatedUserSizeServiceValueオブジェクトは、操作結果を含む推定ユーザサイズの情報を表します。</div>
    <div lang="en">EstimatedUserSizeServiceValue object describes estimated user size information including its operation results.</div>
