FeedDataServiceValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    feedData:
      $ref: 'FeedDataServiceRecord.yaml#/FeedDataServiceRecord'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: FeedDataServiceValue
  description: |
    <div lang="ja">FeedDataServiceValueオブジェクトは、商品情報を格納するコンテナです。</div>
    <div lang="en">FeedDataServiceValue object is a container for storing item list information.</div>
