ContentsKeywordListServiceValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
    contentsKeywordList:
      $ref: 'ContentsKeywordList.yaml#/ContentsKeywordList'
  title: ContentsKeywordListServiceValue
  description: |
    <div lang="ja">ContentsKeywordListServiceValueオブジェクトは、操作結果を含むコンテンツキーワードリストのコンテナを表します。</div>
    <div lang="en">ContentsKeywordListServiceValue object displays the container that includes operation results for contents keyword list.</div>
