DictionaryServiceGeographicLocation:
  type: object
  nullable: true
  properties:
    child:
      type: array
      nullable: true
      items:
        $ref: 'DictionaryServiceGeographicLocation.yaml#/DictionaryServiceGeographicLocation'
      description: |
        <div lang="ja">子要素としてDictionaryServiceGeographicLocationオブジェクトを使用します。</div>
        <div lang="en">DictionaryServiceGeographicLocation object is used.</div>
    code:
      type: string
      nullable: true
      description: |
        <div lang="ja">
        地域コードです。<br>
        「その他」の地域コード(TC-CI-00000073)は、地域ターゲティングでは利用できません。
        </div>
        <div lang="en">
        Geographic code.<br>
        The "Other" geo code (TC-CI-00000073) is not available for geo targeting.
        </div>
    fullName:
      type: string
      nullable: true
      description: |
        <div lang="ja">都道府県名を含む地域名です。</div>
        <div lang="en">Name of places including prefecture name.</div>
    name:
      type: string
      nullable: true
      description: |
        <div lang="ja">市区町村名です。</div>
        <div lang="en">Name of places.</div>
    order:
      type: string
      nullable: true
      description: |
        <div lang="ja">並び順を示す番号です。</div>
        <div lang="en">Number indicating the order of arrangement.</div>
    parent:
      type: string
      nullable: true
      description: |
        <div lang="ja">上位階層の地域コードです。</div>
        <div lang="en">Parent geographic code.</div>
  title: DictionaryServiceGeographicLocation
  description: |
    <div lang="ja">DictionaryServiceGeographicLocationオブジェクトは、地域情報を表します。</div>
    <div lang="en">DictionaryServiceGeographicLocation object displays the geographic location.</div>
