PlacementUrlListServicePage:
  type: object
  nullable: true
  properties:
    totalNumEntries:
      type: integer
      nullable: false
      format: int32
      description: |
        <div lang="ja">取得される項目の総件数です。</div>
        <div lang="en">Total number of items to be retrieved.</div>
    values:
      type: array
      nullable: true
      items:
        $ref: 'PlacementUrlListServiceValue.yaml#/PlacementUrlListServiceValue'
  title: PlacementUrlListServicePage
  description: |
    <div lang="ja">PlacementUrlListServicePageオブジェクトは、getメソッドの実行結果（全Entityのリスト）を保持するオブジェクトです。</div>
    <div lang="en">The objects to keep execution result of get method (All Entity list).</div>
