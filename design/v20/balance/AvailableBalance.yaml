AvailableBalance:
  type: object
  nullable: true
  properties:
    accountId:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">アカウントIDです。</div>
        <div lang="en">The account ID.</div>
    availableBalance:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">推定現金残高です。</div>
        <div lang="en">Estimated cash balance.</div>
  title: AvailableBalance
  description: |
    <div lang="ja">AvailableBalanceオブジェクトは、選択したアカウントの推定現金残高情報を表します。</div>
    <div lang="en">AvailableBalance object describes estimated cash balance information for the selected account.</div>
