AdGroupServiceCustomParameters:
  type: object
  nullable: true
  properties:
    isRemove:
      $ref: 'AdGroupServiceIsRemoveFlg.yaml#/AdGroupServiceIsRemoveFlg'
    parameters:
      type: array
      nullable: true
      items:
        $ref: 'AdGroupServiceCustomParameter.yaml#/AdGroupServiceCustomParameter'
  title: AdGroupServiceCustomParameters
  description: |
    <div lang="ja">AdGroupServiceCustomParametersは、カスタムパラメータの設定を表します。<br>
     このフィールドは、省略可能となります。<br>
    ※SET時、既存の項目を置き換えます。</div>
    <div lang="en">AdGroupServiceCustomParameters displays the setting of custom parameters.<br>
    This field is optional.<br>
    *In SET operation, it will replace the current items.</div>
