GuaranteedSimulationServiceDeliverType:
  type: string
  nullable: true
  enum:
  - INCLUDE
  - EXCLUDE
  - UNKNOWN
  title: GuaranteedSimulationServiceDeliverType
  description: |
    <div lang="ja">
    GuaranteedSimulationServiceDeliverTypeは、ターゲットリスト種別（配信対象・配信除外対象の設定）を表します。<br>
    ADD時、このフィールドは省略可能となります。<br>
    </div>
    <div lang="en">
    GuaranteedSimulationServiceDeliverType displays the type of target list type.<br>
    This field is optional in ADD operation.<br>
    </div>
    <p>* <code>INCLUDE</code> - <span lang="ja">配信対象です。</span><span lang="en">Target.</span></p>
    <p>* <code>EXCLUDE</code> - <span lang="ja">配信除外です。</span><span lang="en">Exclude.</span></p>
    <p>* <code>UNKNOWN</code> - <span lang="ja">未知の値です。</span><span lang="en">Unknown Value</span></p>
