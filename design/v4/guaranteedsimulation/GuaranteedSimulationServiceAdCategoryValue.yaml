GuaranteedSimulationServiceAdCategoryValue:
  type: object
  nullable: true
  properties:
    adCategory:
      $ref: 'GuaranteedSimulationServiceAdCategory.yaml#/GuaranteedSimulationServiceAdCategory'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: GuaranteedSimulationServiceAdCategoryValue
  description: |
    <div lang="ja">GuaranteedSimulationServiceAdCategoryValueオブジェクトは、広告カテゴリー情報を取得するコンテナです。</div>
    <div lang="en">GuaranteedSimulationServiceAdCategoryValue object is a container that retrieves ad category information.</div>
