AdGroupTargetServiceAudienceListTarget:
  type: object
  nullable: true
  properties:
    deliverType:
      $ref: 'AdGroupTargetServiceDeliverType.yaml#/AdGroupTargetServiceDeliverType'
    audienceListName:
      type: string
      nullable: true
      description: |
        <div lang="ja">
        オーディエンスリスト名です。<br>
        このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。
        </div>
        <div lang="en">
        Audience List name.<br>
        Although this field will be returned in the response, it will be ignored on input.
        </div>
    audienceListNameEn:
      type: string
      nullable: true
      description: |
        <div lang="ja">
        オーディエンスリスト名(英語)です。<br>
        共通オーディエンスリストの場合のみ返却されます。<br>
        このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。
        </div>
        <div lang="en">Audience list name (Eng).<br>
        This field is returned when the audience list is shared audience list.<br>
        Although this field will be returned in the response, it will be ignored on input.
        </div>
    audienceListType:
      $ref: 'AdGroupTargetServiceAudienceListType.yaml#/AdGroupTargetServiceAudienceListType'
  title: AdGroupTargetServiceAudienceListTarget
  description: |
    <div lang="ja">
    AdGroupTargetServiceAudienceListTargetオブジェクトは、オーディエンスリストターゲティングの設定情報を保持します。<br>
    ADD、SETおよびREPLACE時、このフィールドは省略可能となります。<br>
    ※targetTypeがAUDIENCE_LIST_TARGETの場合、任意で指定できます。
    </div>
    <div lang="en">
    AdGroupTargetServiceAudienceListTarget object is a container for storing audience list targeting settings.<br>
    This field is optional in ADD, SET and REPLACE operation.<br>
    ∗If targetType is AUDIENCE_LIST_TARGET, this field can be optionally specified.
    </div>
