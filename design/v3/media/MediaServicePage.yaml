MediaServicePage:
  type: object
  nullable: true
  properties:
    totalNumEntries:
      type: integer
      nullable: false
      format: int32
      description: |
        <div lang="ja">取得される項目の総件数です。</div>
        <div lang="en">Total number of items to be retrieved.</div>
    values:
      type: array
      nullable: true
      items:
        $ref: 'MediaServiceValue.yaml#/MediaServiceValue'
  title: MediaServicePage
  description: |
    <div lang="ja">MediaServicePageオブジェクトは、取得される画像に関するエントリーを表します。</div>
    <div lang="en">The MediaServicePage object is a container for storing media entry.</div>
