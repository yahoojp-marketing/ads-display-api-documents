RetargetingListServiceValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
    retargetingList:
      $ref: 'RetargetingList.yaml#/RetargetingList'
  title: RetargetingListServiceValue
  description: |
    <div lang="ja">RetargetingListServiceValueオブジェクトは、取得されたサイトリターゲティングのターゲットリスト情報を表します。</div>
    <div lang="en">The objects to keep the result of method’s execution(1 Entity).</div>
