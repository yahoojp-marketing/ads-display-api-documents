SearchLiftServicePage:
  type: object
  nullable: true
  properties:
    totalNumEntries:
      type: integer
      nullable: false
      format: int32
      description: |
        <div lang="ja">取得される項目の総件数です。</div>
        <div lang="en">Total number of items to be retrieved.</div>
    values:
      type: array
      nullable: true
      items:
        $ref: 'SearchLiftServiceValue.yaml#/SearchLiftServiceValue'
  title: SearchLiftServicePage
  description: |
    <div lang="ja">SearchLiftServicePageオブジェクトは、取得されるサーチリフトに関するエントリーを表します。</div>
    <div lang="en">The SearchLiftServicePage object serves retrieved search lift entry.</div>
