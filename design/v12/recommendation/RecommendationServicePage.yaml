RecommendationServicePage:
  type: object
  nullable: true
  properties:
    totalNumEntries:
      type: integer
      nullable: false
      format: int32
      description: |
        <div lang="ja">取得される項目の総件数です。</div>
        <div lang="en">Total number of items to be acquired.</div>
    values:
      type: array
      nullable: true
      items:
        $ref: 'RecommendationServiceValue.yaml#/RecommendationServiceValue'
  title: RecommendationServicePage
  description: |
    <div lang="ja">RecommendationServicePageオブジェクトは、取得される最適化提案に関するエントリーを表します。</div>
    <div lang="en">RecommendationServicePage object describes the entry on recommendation to be acquired.</div>
