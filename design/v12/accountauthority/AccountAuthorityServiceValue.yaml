AccountAuthorityServiceValue:
  type: object
  nullable: true
  properties:
    accountAuthority:
      $ref: 'AccountAuthority.yaml#/AccountAuthority'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: AccountAuthorityServiceValue
  description: |
    <div lang="ja">AccountAuthorityServiceValueオブジェクトは、アカウント権限情報を格納するコンテナです。</div>
    <div lang="en">The AccountAuthorityServiceValue object is a container for storing account authority information.</div>
