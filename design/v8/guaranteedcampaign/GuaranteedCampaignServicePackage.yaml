GuaranteedCampaignServicePackage:
  type: object
  nullable: true
  properties:
    packageId:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">
        商品IDです。<br>
        このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。
        </div>
        <div lang="en">
        Package ID.<br>
        Although this field will be returned in the response, it will be ignored on input.
        </div>
    packageHistoryId:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">
        商品履歴IDです。<br>
        このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。
        </div>
        <div lang="en">
        Package history ID.<br>
        Although this field will be returned in the response, it will be ignored on input.
        </div>
    packageName:
      type: string
      nullable: true
      description: |
        <div lang="ja">
        商品名です。<br>
        このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。
        </div>
        <div lang="en">
        Package Name.<br>
        Although this field will be returned in the response, it will be ignored on input.
        </div>
  title: GuaranteedCampaignServicePackage
  description: |
    <div lang="ja">
    GuaranteedCampaignServicePackageオブジェクトは、予約型のキャンペーンの商品に関する情報を表します。<br>
    このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。
    </div>
    <div lang="en">
    The GuaranteedCampaignServicePackage object serves package of guaranteed campaign.<br>
    Although this field will be returned in the response, it will be ignored on input.
    </div>
