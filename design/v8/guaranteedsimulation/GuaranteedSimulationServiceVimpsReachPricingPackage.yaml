GuaranteedSimulationServiceVimpsReachPricingPackage:
  type: object
  nullable: true
  properties:
    fixedLifetimeBudget:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">商品価格を表します。</div>
        <div lang="en">Package price.</div>
    viewableReach:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">ビューアブルリーチを表します。</div>
        <div lang="en">Viewable reach.</div>
  title: GuaranteedSimulationServiceVimpsReachPricingPackage
  description: |
    <div lang="ja">
      GuaranteedSimulationServiceVimpsReachPricingPackageは、vimps購入型（リーチ固定）の購入タイプ設定情報を保持します。<br>
      このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。
    </div>
    <div lang="en">
      GuaranteedSimulationServiceVimpsReachPricingPackage holds the purchase type setting information about the vimps purchase type (fixed reach).<br>
      Although this field will be returned in the response, it will be ignored on input. 
    </div>
