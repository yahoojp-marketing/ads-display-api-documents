VideoServiceReturnValue:
  type: object
  nullable: true
  properties:
    values:
      type: array
      nullable: true
      items:
        $ref: 'VideoServiceValue.yaml#/VideoServiceValue'
  title: VideoServiceReturnValue
  description: |
    <div lang="ja">VideoServiceReturnValueオブジェクトは、メソッドの実行結果（全Entityのリスト）を保持します。</div>
    <div lang="en">VideoServiceReturnValue object stores operation results (list of all entities) of method.</div>
