BalanceServiceSelector:
  type: object
  nullable: true
  required:
    - accountId
  properties:
    accountId:
      type: integer
      nullable: false
      format: int64
      description: |
        <div lang="ja">アカウントIDです。</div>
        <div lang="en">Account ID.</div>
  title: BalanceServiceSelector
  description: |
    <div lang="ja">BalanceServiceSelectorオブジェクトは、操作対象となる未消化予算情報を表します。</div>
    <div lang="en">The BalanceServiceSelector object is a container for storing specified blance information.</div>
